# Deploying AXIS for free

Free stack: **Frontend → Vercel**, **Backend → Hugging Face Spaces (Docker)**, **LLM → Groq**.

> ⚠️ Free-tier caveats: HF Spaces storage is **ephemeral** (the DB re-seeds on each cold
> start — user accounts, synced docs, and chats don't survive a restart) and the Space
> **sleeps** after inactivity. For durable data, add a free managed Postgres (Neon/Supabase)
> + hosted vector DB later. This guide gets a live demo running at $0.

---

## 1. Get a free Groq API key
1. Sign up at **https://console.groq.com** (no card).
2. Create an API key → copy it (`gsk_...`).

## 2. Deploy the backend → Hugging Face Spaces
1. Create an account at **https://huggingface.co**.
2. **New → Space** → SDK: **Docker** → name it (e.g. `axis-backend`) → Create.
3. Push this repo's backend to the Space (it uses the root `Dockerfile`):
   ```bash
   git remote add hf https://huggingface.co/spaces/<your-username>/axis-backend
   git push hf main
   ```
   (Or upload the files via the Space's web UI.)
4. In the Space → **Settings → Variables and secrets**, add (as **secrets**):
   | Key | Value |
   |---|---|
   | `AXIS_LLM_BACKEND` | `groq` |
   | `GROQ_API_KEY` | your `gsk_...` |
   | `GROQ_MODEL` | `llama-3.1-8b-instant` |
   | `AXIS_JWT_SECRET` | any long random string |
   | `AXIS_ENCRYPTION_KEY` | a Fernet key — generate with `python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"` |
   | `SMTP_HOST/PORT/USER/PASS/FROM` | (optional) for real OTP emails |
5. The Space builds and serves at `https://<your-username>-axis-backend.hf.space`.
   Test it: open `https://<...>.hf.space/api/health` → should return `{"status":"ok"}`.

## 3. Deploy the frontend → Vercel
1. Edit **`ui/vercel.json`** → replace `REPLACE-WITH-YOUR-SPACE.hf.space` with your Space host
   (e.g. `your-username-axis-backend.hf.space`). Commit + push.
2. At **https://vercel.com**, **Add New → Project** → import this GitHub repo.
3. Set **Root Directory** = `ui`. Framework preset: **Vite** (auto-detected).
4. Deploy. Vercel serves the UI and **proxies `/api/*` to your HF backend** (per `vercel.json`),
   so no CORS config or frontend code changes are needed.

## 4. Use it
Open your Vercel URL → sign up → onboard → ask. Answers are generated by Groq (free).

---

## Notes
- **No Ollama in the cloud** — `AXIS_LLM_BACKEND=groq` replaces it. Locally you can keep `ollama`.
- **Secrets** live in the host (HF Space secrets / Vercel env), never in the repo — `.env`,
  `.axis_key`, and the DB are gitignored and `.dockerignore`d.
- **Persistence upgrade (optional):** swap SQLite → Neon/Supabase Postgres and ChromaDB →
  Qdrant Cloud (free tiers) so accounts/chats/synced docs survive restarts.
